% why do we need inputenc TODO
\RequirePackage[utf8]{inputenc}

% why do we need babel TODO
\RequirePackage[english]{babel}

% for to do notes until the book is done
\RequirePackage[colorinlistoftodos]{todonotes}

% good for consistent style quotes
\RequirePackage[english=american]{csquotes}

% small typographic changes for overall better formatting
\RequirePackage{microtype}

% pretty much most math
\RequirePackage{amsmath}

% math environments pls
\RequirePackage{amsthm}

% actually not sure what this does right now. TODO
\RequirePackage[most]{tcolorbox}

% what does this do? TODO
\RequirePackage{xparse}

% math script fonts
\RequirePackage{mathrsfs}

% nice drawings after 340583 pages of documentation
\RequirePackage{tikz}

% allows for compilation of chapters and sections
\RequirePackage{subfiles}

% bibliography of course
\RequirePackage[backend=biber]{biblatex}
\addbibresource{bib/references.bib}

% nicer links and anything clickable
\RequirePackage{hyperref}
\hypersetup{colorlinks}

% better references with less work
\RequirePackage{cleveref}

% for the occasional multiple column needs
\RequirePackage{multicol}
